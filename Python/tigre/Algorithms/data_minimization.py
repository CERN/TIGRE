import numpy as np
class DataMinimization(object):


    def default_data_minimizing(self):
        return (self.lmbda * 1 / np.array(np.sum(np.expand_dims(
            self.V[:, :, angle_index[j]], axis=0), axis=sumax), dtype=np.float32) * Atb(
            adddim(W[:, :, angle_index[j]], dim_exp) * (adddim(proj[:, :, angle_index[j]], dim_exp)
                                                        - Ax(res, geo, angle, 'ray-voxel')),
            geo, angle, 'FDK'))












    